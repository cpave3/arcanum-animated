"""Outward spiral energy with no central object; the opening stays transparent."""
from animation_fx.primitives.swirl import FPS, FRAMES, SIZE, render_swirl


def render(frame):
    return render_swirl(frame)
